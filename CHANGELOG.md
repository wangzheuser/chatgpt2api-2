# Changelog

## Unreleased

## 2.3.4 - 2026-06-26

+ [新增] 文本链路支持 `reasoning_effort`、`thinking_effort` 和 `reasoning.effort` 透传，`/v1/chat/completions` 与 `/v1/responses` 都会传到 ChatGPT 会话 payload，兼容需要推理强度参数的上游模型。
+ [新增] 文本缓存键纳入 `reasoning`、`reasoning_effort`、`thinking_effort`，避免不同推理强度的请求命中同一份缓存。
+ [新增] 新增日志保留天数配置 `log_retention_days`，服务启动和后台定时任务会自动清理过期 `data/logs.jsonl`，减少长期运行后的日志体积。
+ [新增] 系统设置页加入“日志自动清理”入口，支持在前端直接配置日志保留天数。
+ [优化] 设置页说明文案改为问号悬浮提示，账号刷新、图片清理、轮询超时、上游流超时、单账号图片并发、Cloudflare 清障、代理运行时等字段说明更清爽。
+ [修复] 图片账号选择失败和远程确认额度耗尽分开处理，只有远程确认额度耗尽才返回 429；账号池暂不可用、预检失败或上游波动统一返回可重试错误，避免有余额时误报额度不足。
+ [修复] 本地图片额度扣减到 0 不再直接把账号写成“限流”，改为标记额度未知并交给下一次远程预检确认，避免本地估算与远程真实额度不同步时把账号锁死。
+ [优化] 自动移除限流账号的触发口径收紧为“远程确认额度耗尽”，并将日志文案改为“自动移除额度耗尽账号”，减少网络波动或本地估算导致误删。
+ [优化] 图片错误响应增加 `no_account` 友好文案，区分“账号池暂不可用/并发/上游波动”和“额度确实耗尽”两类场景。
+ [优化] 仓库配置示例改用 `config.example.yaml`，删除仓库内默认 `config.json`，避免把本地密钥、代理和存储配置误提交；README 补充运行时仍使用本地 `config.json` 的说明。
+ [优化] Dockerfile 改为打包 `config.example.yaml`，compose 示例补齐 `GIT_AUTH_KEYS_FILE_PATH`，Git 存储可单独指定账号文件和用户密钥文件路径。
+ [优化] 安装脚本改为编号式交互选择：语言、Docker/Python 运行模式、json/sqlite/postgres/git 存储后端都可以按数字选择，减少手动输入错误。
+ [优化] 安装脚本补齐 Git 存储参数 `GIT_REPO_URL`、`GIT_TOKEN`、`GIT_BRANCH`、`GIT_FILE_PATH`、`GIT_AUTH_KEYS_FILE_PATH`，并按不同存储后端引导填写数据库或 Git 配置。
+ [优化] `.env.example`、`docker-compose.yml`、`docker-compose.local.yml`、`docker-compose.warp.yml` 同步新的 Git 存储和配置模板字段，保持部署入口一致。

## 2.3.3 - 2026-06-26

+ [修复] 修复 Docker 或挂载盘环境下保存设置、代理配置时，原子替换 `config.json` 可能触发 `Device or resource busy` 并返回 500 的问题，失败时会自动改用直接覆盖保存。
+ [优化] 系统设置和代理管理保存失败时返回更清晰的中文错误，前端不再裸显示 `Internal Server Error`。

## 2.3.2 - 2026-06-25

+ [优化] 收口 token 失效处理入口：文本流、图片流和账号刷新中仅在检测到 token_invalidated、token_revoked 等鉴权失效时，才统一进入异常账号处理。
+ [优化] 异常账号优先尝试“异常后自动重登”，重登失败、密码错误或账号停用/封号后，再按“自动移除异常账号”开关决定删除或保留异常原因。
+ [优化] 导入刷新继续保留异常确认弹窗；普通对话失败、图片生成失败、断流、超时、内容策略拒绝和限流不会被当成异常账号处理。

## 2.3.1 - 2026-06-25

+ [修复] 修复调用日志详情中失败日志步骤明细无法手动折叠的问题；无图片链路埋点的文本对话不再显示空的“步骤耗时”。
+ [修复] 修复图片管理磁盘剩余固定显示 MB 的问题，改为按大小自动显示 GB/MB。
+ [修复] 图片列表补齐文件名、过期状态和保留天数字段，清理过期图片后同步校验索引，减少旧图片删除后仍占位的情况。
+ [优化] 账号列表对异常账号延迟移除机制增加前端状态提示，避免自动移除异常账号开启后看起来没有生效。

## 2.3.0 - 2026-06-25

+ [修复] JSON 持久化收口为原子写入和 `.bak` 恢复，降低配置、账号和任务文件在异常中断时损坏的风险。
+ [修复] 设置保存改为白名单字段写回，避免页面误覆盖账号组、代理组等非本页配置。
+ [优化] 前端设置与存储链路同步收口，统一版本口径和本地持久化行为。

## 2.2.6 - 2026-06-25

+ [修复] 修复概览中心被路由缓存后切回页面仍显示旧统计和旧图表的问题，进入页面时会重新拉取最新数据并显示加载状态，避免旧数据先闪一下再刷新。
+ [优化] 统一账号管理、系统设置、代理管理等 KeepAlive 页面的激活刷新逻辑，切回页面时直接刷新当前数据，编辑、保存、导入或测试中不会覆盖当前输入。
+ [优化] 优化控制台顶部路由加载线为完整横向反馈，并清理图片管理页残留的 30 秒激活刷新逻辑，让页面切换加载状态更一致。

## 2.2.5 - 2026-06-25

+ [优化] 调用日志详情补充图片任务后台链路埋点，生图任务页触发的请求也会记录 `call_id`、账号、会话和关键阶段耗时。
+ [优化] 日志详情新增图片链路分段耗时条，按入口账号、上游准备、上游生成、解析轮询、下载和重试等待区分颜色，并支持慢阶段与异常阶段标记。
+ [优化] 日志详情步骤明细改为默认折叠，失败、断流或超长请求自动展开，减少正常成功日志的视觉噪音。
+ [优化] 日志详情请求身份和诊断字段去重，成功日志不再展示重复的代理来源、代理 Hash 和完成阶段，时间范围改为跨列左对齐展示。

## 2.2.4 - 2026-06-24

+ [修复] 图片生成错误提示友好化改为独立设置标签控制，默认关闭时保持原始错误返回，开启后可自定义断流、轮询超时、额度不足等提示文案。
+ [优化] 实时监控补充入口账号合计、上游准备合计、准备占比和最慢准备项，方便判断高并发请求卡在入口、账号还是上游准备阶段。
+ [优化] 实时监控新增活跃请求阶段分布，方便高并发下判断请求集中卡在等待入口、等待账号、等待出口、上游准备、上游生成或解析/轮询。
+ [优化] 图片链路补充出口来源、出口等待和本地拒绝/繁忙埋点，慢请求与活跃请求能看到代理出口和账号等待情况。
+ [优化] 图片生成错误提示友好化，针对上游断流、轮询超时、返回文本但无图、额度不足和本地繁忙/无可用账号返回更清晰的中文提示。

## 2.2.3 - 2026-06-23

+ [新增] 系统设置新增“上游流超时”，用于控制 ChatGPT 生图 SSE 流最长等待时间，和图片结果轮询超时分开配置。
+ [优化] 实时监控新增上游异常、响应整理和单图内部耗时指标，慢请求卡片扩展为 16 项，更容易看出卡在上传、上游生成还是解析/轮询。
+ [优化] 图片链路补充 HTTP/2/SSE 流中断耗时埋点，遇到 `curl (92)`、HTTP/2 stream reset 等问题时不再长期落到“未标记”。
+ [优化] 图片结果轮询的重试分支改为尊重 `image_poll_timeout_secs` 配置，减少隐藏超时口径导致的误判。

## 2.2.2 - 2026-06-23

+ [优化] 实时监控补齐图片链路阶段：选择账号、上传图片、初始化上游、获取令牌、准备会话、启动生成和上游生成中。
+ [优化] 实时监控新增当前阶段耗时，慢请求不再长期误显示为“获取账号”，方便定位 504 或排队卡点。
+ [优化] 监控页补充上传、初始化、令牌、准备会话、启动生成等耗时指标，并覆盖 new-api 直连 `/v1/chat/completions` 生图请求。

## 2.2.1 - 2026-06-23

+ [修复] 修复单个账号刷新后账号被移除或判定异常时，前端仍可能显示刷新成功的问题，刷新进度会携带异常并提示具体失败原因。
+ [修复] 修复 GPTMail 状态检测和公共 Key 刷新没有继承注册账号代理的问题，避免检测链路与真实注册读取邮箱链路不一致。
+ [优化] GPTMail 状态缓存加入代理维度，并在代理 CONNECT 隧道失败时给出明确提示，方便定位公开 Key 检测失败原因。

## 2.2.0 - 2026-06-22

+ [新增] 注册账号新增 GPTMail 邮箱来源，支持公共测试 Key 和自定义 Key 两种模式，并可检测额度、剩余次数和每日重置时间。
+ [新增] GPTMail 公共 Key 支持 reveal 后写入后端缓存，注册时可自动获取当前可用 Key，前端仅展示脱敏 Key 提示。
+ [优化] GPTMail 公共 Key 按官方每日重置时间自动刷新：重置时间请求一次，重置时间 +5 分钟再保底刷新一次，避免继续使用旧 Key。
+ [优化] GPTMail 支持已知域名本地拼接模式，减少生成邮箱接口调用；注册页会在切换或删除邮箱来源时清理旧状态，避免额度信息串位。
+ [修复] 修复 GPTMail 自定义 Key 检测时误用公共 Key 重置时间的问题，自定义 Key 仅展示自身接口返回的总额度、已用和剩余信息。

## 2.1.1 - 2026-06-18

+ [修复] 修复图片管理在本地图片较多时一次性扫描和统计导致 `/api/images` 请求超时的问题，后端改为真实分页并延后加载存储统计。
+ [修复] 修复日志管理分页、筛选和搜索参数只在前端生效的问题，`/api/logs` 改为后端分页与条件过滤，并优化大日志文件读取。
+ [优化] 实时监控慢请求卡片补充完整阶段拆解，显示解析/轮询、下载、重试等待、单图链路、单图总计和未标记耗时。
+ [优化] 图片链路新增解析失败、下载失败和退避重试等待埋点，方便定位总耗时大但上游耗时不高的卡点。
+ [优化] `config.json` 补齐控制台使用的公开展示、图像生成、额度限制、运行容量和代理组默认字段，发布默认代理保持为空。

## 2.1.0 - 2026-06-18

+ [新增] 新增“实时监控”页面和 `/api/monitor/realtime` 接口，用于查看容器内存中的实时请求窗口、入口排队、线程池等待、账号等待、上游生成、图片解析、下载和保存耗时。
+ [新增] 图片链路新增轻量性能追踪，为图片生成、图片编辑、Responses 生图和 Chat Completions 生图请求注入 `call_id`，并在调用日志中记录关键阶段耗时。
+ [优化] 优化控制台路由切换体验，延迟并分散预加载较重页面，新增路由加载进度与日志页初次加载状态，减少页面切换时的卡顿感。
+ [优化] Dockerfile、`docker-compose.yml` 和 `docker-compose.warp.yml` 增加 `TZ=Asia/Shanghai` 与 `CHATGPT2API_THREAD_TOKENS` 默认配置，容器内时间和后端线程池容量口径更明确。
+ [优化] 统一日志、概览中心、图片管理、图片任务和调用记录的北京时间处理，修复 Docker 环境下 24 小时/7 天/30 天统计时间偏移、时间桶显示错位的问题。
+ [优化] 优化 `/v1/chat/completions` 的生图识别逻辑，`gpt-image-*` 等聊天生图请求会按“聊天生图”记录，并补充图片访问地址，避免被当作普通文本对话导致结果不显示图片。
+ [优化] 优化日志图片 URL 提取，支持从 Markdown 图片文本中提取 `/images/`、`/image-thumbnails/` 和远程图片地址，日志详情更容易预览图片结果。
+ [优化] 统一代理优先级：账号个人代理 > 账号组代理组 > 显式任务代理 > 全局代理 > 稳定代理运行时 > 直连；`direct` 会截断回退，`group:<id>` 会解析代理组节点。
+ [优化] 代理组改为按时间窗口轮换，默认 5 分钟；账号请求在窗口内保持节点粘性，避免同一账号多次请求频繁换 IP。
+ [优化] 代理管理页新增代理组轮换间隔配置，可设置为 `0` 表示每次新请求都轮询。
+ [优化] 注册账号页的注册代理从裸输入框改为统一选择器，支持“使用全局代理 / 直连 / 代理组 / 自定义代理”，并继续兼容原 `register.proxy` 配置字段。
+ [优化] 邮箱请求层复用统一代理解析，注册机读取邮箱、Graph 邮件和临时邮箱接口可以正确识别 `direct`、`group:<id>` 和自定义代理。
+ [修复] 修复创建或编辑代理组后弹窗不自动关闭的问题。

## 2.0.1 - 2026-06-18

+ [修复] 修复 Microsoft passwordless 验证码校验遇到 `invalid_state` 时直接失败的问题，会话失效后自动重新发起一次登录验证码流程。
+ [修复] 修复 Microsoft passwordless 登录会话失效重试时清理 auth cookie 触发 `'str' object has no attribute 'domain'` 的问题。
+ [优化] 优化注册账号页 Microsoft 邮箱池维护入口，支持在“更多维护”中释放并重试异常邮箱状态。
+ [优化] 优化 `outlook_token` 邮箱池导入统计、异常状态文案和 Microsoft 429 限流退避，减少异常状态误判。

## 2.0.0 - 2026-06-17

+ [新增] 新增 Vue 控制台前端，覆盖概览中心、账号管理、日志管理、图片管理、代理管理、注册账号、图像创作、调试中心和系统设置。
+ [新增] 新增图像创作入口，支持对话式生图、多图数量、比例/分辨率参数、历史记录删除和图片下载。
+ [新增] 新增统一的前端组件与交互基线，包括加载状态、分页、分段切换、下拉菜单、弹窗、详情图片预览和批量操作进度。
+ [优化] 优化账号导入、CPA JSON 导入、异常账号确认移除、账号组/代理展示、图片额度和列表性能。
+ [优化] 优化日志管理和概览图表数据维度，修复时间范围切换后平均响应时间、模型请求分布等统计污染问题。
+ [优化] 优化注册账号页面和 `outlook_token` 邮箱读取流程，补齐 Microsoft 邮箱验证码读取、状态统计和注册日志展示。
+ [优化] 系统设置回归真实 `config.json` 配置，补齐用户密钥、WebDAV、AI 审核、R2 备份、CPA 和 Sub2API 管理入口。
+ [优化] 发布镜像和后端静态目录切换到 Vue 控制台产物，避免继续加载旧版前端。
+ [优化] 清理仓库发布内容，测试目录、测试脚本和测试文档只保留本地；运行日志、runtime、standalone 和临时文件改为忽略。

## 1.5.0 - 2026-06-13

+ [新增] 新增 WARP / Privoxy / FlareSolverr 清障方案，注册遇到 Cloudflare 拦截后可刷新 clearance 并重试。
+ [新增] 新增 `outlook_token` 邮箱池，支持 Outlook/Hotmail 注册验证码读取。
+ [新增] 新增网页搜索兼容接口、图片编辑 mask 和图片任务相关能力。
+ [优化] 更新 sentinel/PoW 获取方式，提高上游请求兼容性。
+ [优化] 调整代理优先级和注册请求重试逻辑。

## 1.4.1 - 2026-06-03

+ [新增] 账号刷新改为异步模式，支持前端轮询刷新/重新登录进度。
+ [新增] 号池管理页面新增重新登录功能，支持密码登录恢复异常账号。
+ [新增] 刷新后自动重新登录异常账号（可在设置页开启）。
+ [新增] 图片生成支持并行模式，多张图片使用独立线程和账号同时生成。
+ [新增] 图片轮询超时自动换账号重试（最多4次），连接超时同账号递增等待重试。
+ [新增] 图片二次确认机制与先check再hit可配置化，关闭后可跳过等待直接返回结果。
+ [新增] 图片任务进度追踪，显示当前生成步骤（上传/预热/获取token/生成中等）。
+ [新增] 图片超时后续轮询功能，前端显示"继续等待"按钮。
+ [新增] 设置页新增图片二次确认、超时等待时间、自动重新登录等配置项。
+ [优化] 优化生图页面滚动加载性能，图片懒加载、会话切换滚动位置保存与恢复。

## 1.4.0 - 2026-05-31

+ [新增] 新增AI生成可编辑PSD文件逆向。
+ [新增] 新增AI生成可编辑PPT文件逆向。

## 1.3.1 - 2026-05-30

+ [新增] 新增ChatGPT搜索调试、Skills。

## 1.3.0 - 2026-05-30

+ [新增] 新增ChatGPT搜索接口逆向。

## 1.2.4 - 2026-05-30

+ [新增] 添加聊天补全缓存与重复请求合并。
+ [新增] 新增无限画布一键跳转功能

## 1.2.3 - 2026-05-29

+ [新增] 新增账号级代理。
+ [修复] 修复503异常信息、前端邮箱换行问题。

## 1.2.2 - 2026-05-29

+ [新增] 新增Codex链路生图、支持2k,4k。
+ [新增] 支持RT刷新账号信息。

## 1.2.0 - 2026-05-28

+ [新增] 当前版本基线，包含 Web 面板、画图、号池管理、注册机、图片管理、日志管理和设置能力。
+ [新增] 前端版本号支持点击查看版本更新弹窗，展示当前版本、最新版本和更新日志。
+ [优化] 优化注册机效率，成功率大幅提高。
+ [优化] 优化生图页面配置选项。
